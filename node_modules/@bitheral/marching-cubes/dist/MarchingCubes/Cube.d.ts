import { Vector3, Vector4, BufferGeometry } from "three";
import { Volume } from "./Volume";
export interface MeshData {
    vertices: number[];
    normals: number[];
}
export declare class Cube {
    private position;
    static cornersIndex: Vector3[];
    corners: Vector4[];
    cornerDensity: number[];
    private volume;
    private geometry;
    private noise;
    constructor(volume: Volume, position: Vector3);
    private generateValues;
    private updatePosition;
    buildMesh(updateValues?: boolean): BufferGeometry;
}
export type CubeType = Cube;
export default Cube;
